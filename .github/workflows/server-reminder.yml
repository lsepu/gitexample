name: Recordatorio para actualización en servidores

on:
  pull_request:

jobs:
  mostrar-recordatorio:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3
    
      - name: Recordatorio
        run: |
          echo "⚠️ Recuerda actualizar en todos los servidores."
